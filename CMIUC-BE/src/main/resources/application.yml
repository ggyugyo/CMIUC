# JPA Configuration
spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  profiles:
    include:
      - db
      - oauth
      - redis
      - jwt

# Hibernate SQL Debugging
logging:
  level:
    org:
      hibernate:
        sql: debug